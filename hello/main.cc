#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false); cin.tie(0);
    // Very big sum 
    int tt;
    long long sum = 0;
    while(tt--){
      long long num;
      cin>>num;
      sum = sum+num;
    }
    cout<< sum;
    return 0;
}