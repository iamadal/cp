#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false); cin.tie(0);
    
    int tCase;
    cin>>tCase;

    while(tCase--){
       string data;
       int count;
       cin >> data;
       count = data.length();
       for(int i=0;i<count;i++){
           
       }
       
    }

    return 0;
}